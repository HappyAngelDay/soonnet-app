{
  "name": "soonnet-app",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "git add . && git commit -m \"Deploy\" && npm run build && echo > dist/.nojekyll && git subtree push --prefix dist origin gh-pages"
  },
  "dependencies": {
    "framework7": "^8.3.4",
    "framework7-vue": "^8.3.4"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.2.4",
    "gh-pages": "^6.0.0",
    "vite": "^5.4.19"
  },
  "homepage": "https://happyangelday.github.io/soonnet-app",
  "repository": {
    "type": "git",
    "url": "https://github.com/HappyAngelDay/soonnet-app.git"
  }
}